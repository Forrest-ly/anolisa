name: tokenless
version: "0.7.5"
description: "Token-Less context compression for AgentScope SDK — response compression, TOON encoding, command rewriting, and Tool Ready environment pre-check"
author: ANOLISA
requires_env: []
provides_hooks:
  - middleware
